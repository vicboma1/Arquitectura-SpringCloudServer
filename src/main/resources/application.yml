server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vicboma1/Arquitectura-SpringCloudConfig
          timeout: 10

  profiles:
    active: dev